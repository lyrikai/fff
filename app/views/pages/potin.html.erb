<nav class="navbar navbar-expand-lg navbar-light bg-light d-flex flex-column  align-items-center">
<a class="navbar-brand" href="<%= root_path %>">Home</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link active" href= <%= pages_team_path %> >Team <span class="sr-only"></span></a>
      <a class="nav-item nav-link" href="<%= pages_contact_path %>">Contact</a>

    </div>
  </div>
</nav>


<% potin = Gossip.find(params[:id]) %>


<div class="container">

<br>

<div class="card text-center">
  <div class="card-header">
  Potin n°<%= potin.id  %>
  </div>
  <div class="card-body">
    <h5 class="card-title"><%= potin.title  %></h5>

    <p class="card-text"><%= potin.content  %></p>
    <a href="../author/<%=potin.user.id - 1 %>" class="btn btn-primary">Ecrit par: <%= potin.user.first_name %></a>
  </div>
  <div class="card-footer text-muted">
  <%= potin.created_at  %>
  </div>
</div>
</div>